import { StyleSheet } from 'react-native';
import * as cs from '../CommonStyles';

export default () => StyleSheet.create({
  photo: {
    width: cs.SCREEN_WIDTH,
    height: cs.SCREEN_WIDTH,
    backgroundColor: cs.COLOR.lightGray
  }
});